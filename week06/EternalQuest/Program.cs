using System;

class Program
{
    static void Main(string[] args)
    {
        // EXTRA CREDIT: I added all core logic + a clean menu + save/load system.
        // Exceeding requirements: easy extension capability, full OOP structure.
        //I also added an additional Goal,Progress Goal and included all new core logic required for the goal as well.

        GoalManager gm = new GoalManager();
        gm.Start();
    }
}
